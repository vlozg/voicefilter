experiment:
  use_cuda: True
  
  dataset:
    name: "generate" 
    test:
      file_path: "datasets/recipes/Old_VNGenerateDataset.csv"
      detail: ["vin", "zalo-train", "zalo-test"]
      audio_len: 3
      size: 5000