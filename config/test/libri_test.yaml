experiment:
  use_cuda: True
  
  dataset:
    name: "generate" 
    test:
      file_path: "datasets/recipes/LibriGenerate_test.csv"
      detail: ["librispeech-test"]
      audio_len: # varying audio length
      size: 10000