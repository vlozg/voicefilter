experiment:
  use_cuda: False
  
  dataset:
    name: "generate" 
    test:
      file_path: "datasets/recipes/LibriGenerate_eval.csv"
      detail: ["librispeech-test"]
      audio_len: 3.0
      size: 100